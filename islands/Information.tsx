import Component from "site/components/ui/Information.tsx";
import type { Props } from "site/components/ui/Information.tsx";

function Island(props: Props) {
    return <Component {...props} />;
}

export default Island;
